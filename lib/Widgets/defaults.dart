import 'package:flutter/material.dart';

class Defaults {
  static const selectedTilecolor = Color.fromARGB(255, 195, 195, 219);
  static const bottomNavBackgroundColor = Color.fromARGB(242, 2, 2, 2);
  static const Color bottomNavItemColor = Colors.blueGrey;
  static const Color bottomNavItemSelectedColor =
      Color.fromARGB(255, 38, 74, 236);

  static const bottomNavItemIcon = [
    Icons.lock_outline,
    Icons.calendar_view_day_sharp,
    Icons.support_agent_outlined,
    Icons.settings_outlined,
  ];
  static const bottomNavItemText = [
    "Login",
    "Services",
    "Support",
    "Settings",
  ];
}
